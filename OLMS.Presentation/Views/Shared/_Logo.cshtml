@if (!(User?.Identity?.IsAuthenticated ?? false))
{

	<div class="logo">
		<a href="@Url.Action("Index", "Home")">
			<img src="~/assets/images/logo.png" alt="logo" width="120" height="60" viewBox="0 0 120 60" fill="none">
		</a>
	</div>

}
else
{

	<div class="logo">
		<a href="@Url.Action("Index", "Home")">
			<img src="~/assets/images/logo.png" alt="logo" width="120" height="60" viewBox="0 0 120 60" fill="none">
		</a>
	</div>

}


<style>
	.logo{
		font-size: 48px;
		color: black;
		font-weight: 450;

		display: flex;
		align-items: center;
		justify-items: center;
	}
</style>